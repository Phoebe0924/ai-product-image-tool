# LightPic Demo Quality Review

> Scope: code-level review only. Based on current `src/app/page.tsx`, `src/app/api/analyze/route.ts`, `src/app/api/generate/route.ts`, `src/lib/canvas/template1.ts`, and current local project docs. No API keys were read.

## 当前 4 张图生成链路概览

当前链路是两段式：

1. `/api/analyze` 用 Claude Vision 分析上传图，输出 `image_plan`。每个 plan item 包含 `role`、`layout_variant`、`title`、`purpose`、英文生成 prompt 和中文 `copy`。
2. 前端 `startGenerate()` 串行处理 4 张图。每张图先调用 `/api/generate` 拿 Replicate 原始生成图，再调用 `synthesizeTemplate1()` 做 Canvas 文案合成。

4 张图的角色在 `analyze/route.ts` 中固定：

- `scene-1`：主视觉图，强制 `layout_variant: "A"`。
- `scene-2`：质地/妆感图，强制 `layout_variant: "C"`。
- `scene-3`：使用场景图，强制 `layout_variant: "D"`。
- `scene-4`：卖点信息图，强制 `layout_variant: "B"`。

后端还有 `sanitizeImagePlan()`，如果 Claude 返回的角色或版式不符合固定顺序，会被纠正；如果命中真人、前后对比、实验室、成分可视化等禁词，会替换成 `SAFE_FALLBACKS` 里的安全兜底 prompt。

真正影响最终 demo 观感的不是单一 prompt，而是三层叠加：

- Claude 规划 prompt：决定每张图的原始画面。
- `/api/generate` 追加硬约束：再次限制产品保真、禁止人物、禁止文字、禁止复杂内容。
- Canvas 模板：把所有结果统一加上大标题、黑色渐变、底栏或胶囊卖点。

## 每张图的定位是否清晰

### 1. 主视觉图

定位基本清晰：产品正面、主体最大、白底或极简背景，强调包装质感和第一印象。

问题是代码里同时要求“主视觉图允许少量文字”和 `/api/generate` 又要求“输出图绝对无文字”，最后文字由 Canvas 强加。结果容易变成：底图像干净产品照，但上层出现非常大的营销字和黑色渐变，像模板海报，不像真实可上架主图。

Demo 判断：适合作为第一张，但应最克制。它应该让朋友第一眼看到“商品没被改坏”，而不是先看到大字压图。

### 2. 质地/妆感图

定位不够稳定。当前 prompt 要求“产品局部特写，展示瓶身材质、质地细节”，但模型拿到的是完整产品包装图，不一定知道真实膏体、粉质、乳液质地是什么。为了安全，prompt 又禁止成分、科技、真人、皮肤和效果对比，这会让“质地/妆感”只能退化成瓶身近景或材质特写。

Demo 判断：如果上传的是面霜、粉底、口红这类需要质地表达的商品，这张容易显得“说是质地，但画面并没有真正展示质地”。如果模型幻想膏体或粉质，又会有商品真实性风险。

### 3. 使用场景图

定位较清晰：梳妆台、浴室台面、晨间护肤桌面、旅行化妆包等生活场景，不出现人物，道具不超过 2 件。

问题是合法场景列表是通用品类场景，不根据具体包装、价位、风格做足够窄的选择。比如东方草本、高端奢华、少女甜美、专业功效，理论上应有不同桌面材质、光线和道具，但当前 prompt 只给了宽泛生活场景。模型容易生成“AI 风干净桌面”，看起来完整但不真实。

Demo 判断：这是最能提升 demo 的一张，但需要更固定、更真实、更少装饰的默认场景。

### 4. 卖点展示图

定位最模板化。当前要求产品偏右、左侧留白、背景干净，Canvas `layout B` 又固定左侧大标题、两条胶囊 bullet、底部副标题。它很像“自动套版卖点图”，但不像一个真实卖家会直接用的商品图。

Demo 判断：这张最容易暴露 AI 模板感。如果录屏里出现标题大、黑色渐变重、胶囊标签泛泛，会让用户觉得“这个工具只是把图套进模板”。

## 当前 prompt 的问题

### 保留原商品主体

有明确要求，而且重复了两次：

- `analyze/route.ts` 要求每条 prompt 开头写入 `CRITICAL PRODUCT FIDELITY`。
- `generate/route.ts` 在请求 Replicate 前再次追加产品保真规则。

这点方向正确，但实际效果可能仍不足。原因是 prompt 只说“不要改结构/瓶盖/logo/标签”，但没有要求“保持原图商品抠图式主体、不要重新渲染商品本体”。对 image-edit 模型来说，它仍可能整体重绘包装，导致瓶盖比例、标签字形、材质质感变化。

### 构图控制

有控制，但偏粗：

- 产品至少占画面高度 50%。
- 主视觉居中。
- 卖点图产品偏右。
- 场景图道具不超过 2 件。

不足是没有给稳定镜头语言，比如“straight-on product photo, eye-level, 85mm product photography, no dramatic perspective, no floating object”。目前模型可能用海报式透视、夸张光影或过度景深。

### 背景控制

有白底、极简、梳妆台、浴室台面等控制，但“真实电商背景”的约束不够具体。当前背景描述偏“干净、简洁、极简渐变”，这会把结果推向 AI 海报，而不是淘宝/拼多多真实商品图。

Demo 优先时，背景应该更窄：白色亚克力台面、浅灰墙面、自然窗光、浴室白瓷台面、米白梳妆台。少用“subtle gradient”“premium”“atmosphere”这类容易海报化的词。

### 文字/标签控制

底图层面控制很强：`/api/generate` 明确要求无文字、无中文、无水印、无标签、无 overlay。

但最终图层面反而文字很多，因为 Canvas 一定会尝试合成文案。`drawLayoutA/B/C/D` 都有大标题或底栏；B/D 还会画 bullet 胶囊。也就是说，当前“禁止模型生成文字”解决的是乱码问题，但没有解决“最终图文案太模板”的问题。

### 避免重新设计商品

有明确禁止重新设计包装，但缺少“如果无法安全编辑，就保持原图产品区域不变”的退路。当前模型被要求改变背景、光照、环境，同时保持商品完全不变；这是高难度任务。只要模型重绘商品，demo 就会被真实卖家立即看出不可靠。

### 电商真实感 vs AI 海报感

当前 prompt 里有“拼多多电商风格”“真实中文电商”“生活场景真实感”等要求，但这些要求太抽象。视觉上真正把结果拉向模板感的是：

- “产品必须大且 dominant”在四张图里重复，导致构图差异有限。
- “背景干净、极简渐变、留白供文案叠加”容易生成海报底图。
- Canvas 统一加黑色渐变、大白字、胶囊标签，使四张图风格非常一致。

## 当前视觉结果的问题

### 产品卖点分析被过度用于生成

有这个倾向。`analyze` 要求全局 `selling_points`，每张图还要求独立 `copy`，前端又提供“核心卖点”“文案编辑”“场景说明”。最终视觉上，卖点被叠加到每一张图里，结果像一组 AI 文案模板，而不是一组真实商品图。

具体表现：

- 文案量偏多：主标题、副标题、2 条 bullets、brand、new_badge 都可能进入 Canvas。
- 标签形式偏 AI：黑色半透明胶囊、巨大标题、底部黑栏，会显得像自动化模板。
- 场景和商品容易不匹配：安全场景列表很泛，模型可能给任何护肤品都套“梳妆台/浴室台面/晨间桌面”。
- 视觉目标不够聚焦：一套图同时想做主图、质地、场景、卖点，但每张都“产品大、背景干净、可叠文案”，差异化主要靠 Canvas 版式，而不是真实拍摄意图。

### 目前最影响 demo 观感的 5 个问题

1. **Canvas 模板感过强。** `layout A/B/C/D` 都有固定黑色渐变、大标题或胶囊标签，尤其 B/D 很像自动套版，不像真实店铺直接使用的商品图。
2. **产品保真风险仍然是第一信任问题。** Prompt 禁止改包装，但模型如果重绘瓶盖、标签、比例或材质，用户会觉得“商品被 AI 改坏了”，比背景不好看更致命。
3. **四张图的视觉差异来自版式，而不是真实拍摄策略。** 底图都被要求大产品、简单背景，最终差异靠标题位置和渐变条，容易让朋友觉得“完整但没灵魂”。
4. **质地/妆感图概念不稳。** 没有真实质地素材时，模型只能做包装特写或幻想质地；这张在 demo 中容易显得不专业。
5. **录屏节奏暴露等待和不确定性。** 4 张串行生成时间长，且生成中逐张出现。如果某一张明显变形或模板感重，录屏会把不稳定性完整展示出来。

## 最适合 demo 的商品类型建议

Demo 优先不是证明全品类能力，而是选择最能稳定出片的输入。

建议优先：

- **单瓶护肤品**：面霜、乳液、精华、防晒。包装形状简单，正面标签清楚，适合主视觉和桌面场景。
- **浅色/白色/米色/银色包装**：更适合白底、浴室台面、梳妆台，不容易和黑色渐变冲突。
- **瓶身或罐身轮廓清晰的产品**：不要透明瓶内复杂液体、不要多件套、不要异形包装。
- **正面干净原图**：商品居中、背景简单、无遮挡、logo 和标签可读。
- **卖点安全的商品**：日常保湿、清爽不黏、温和护理、细腻肤感这类，不要祛痘、美白、医美、功效对比。

不建议用于当前 demo：

- 多件套礼盒，容易被模型增删瓶子。
- 口红/眼影盘等强色号商品，容易触发色号和妆效幻想。
- 包装上文字很多的小瓶，模型容易改坏标签。
- 黑色高反光包装，Canvas 黑色渐变会压主体。
- 需要真人肤感证明的底妆类，当前 prompt 禁止人物/皮肤，展示空间受限。

## 最适合 demo 的场景组合建议

为了录制稳定 demo，建议把“4 张图”从泛化方案收窄为固定、真实、低风险的一组：

1. **白底主图**：纯白或浅灰背景，产品正面居中，少或无叠字。目标是证明商品主体保真。
2. **台面质感图**：大理石/亚克力/米白台面，产品 3/4 角度或轻微侧光，不幻想膏体。目标是提升质感，而不是展示不存在的成分。
3. **浴室或梳妆台场景图**：白瓷台面、干净镜面、自然窗光，最多 1 个毛巾/托盘道具。目标是真实生活使用场景。
4. **轻文案卖点图**：产品偏右，左侧留白，但只放一个短主标题和一个副标题，去掉胶囊 bullet 和重黑渐变。目标是可用于 demo 展示“可编辑文案”，不追求最终商用模板。

展示顺序建议：

1. 先展示白底主图：建立信任。
2. 再展示场景图：让用户感到“能变成真实商品图”。
3. 再展示台面质感图：补充高级感。
4. 最后展示轻文案卖点图：展示 AI 文案能力，但不要让它主导第一印象。

## 1 天内可完成的最小优化计划

以下是建议修改计划，等待确认后再动代码。

### P0：降低模板感

- 对 demo 模式先减少 Canvas 文字密度：主视觉图不叠大字，或只保留很小品牌/副标题。
- 卖点图只保留主标题 + 副标题，暂时去掉胶囊 bullets、圆形 badge、重黑渐变。
- 将 `layout D` 的底部黑栏改成更轻的白/半透明浅色信息条，避免像廉价海报。

### P0：收窄 prompt 场景

- 把默认场景固定为白底、浅灰台面、浴室白瓷台面、米白梳妆台，不让模型自由扩散。
- 在 prompt 中加入“realistic ecommerce product photography, not poster, not campaign ad, not surreal, not 3D render”的明确约束。
- 加入更强保真描述：“keep the original product region visually unchanged when possible; do not repaint label typography”。

### P1：调整 4 张图定义

- 将“质地/妆感图”改成更稳的“包装质感/细节图”，避免模型幻想膏体、粉质或妆效。
- 将“卖点信息图”改为“轻文案卖点图”，只服务 demo，不追求高密度电商详情页。
- 结果标签统一为 `主视觉图 / 包装质感图 / 使用场景图 / 轻文案卖点图`，减少“痛点图”这类泛化表达。

### P1：优化录屏展示

- 结果区优先展示更大的图片，减少右侧说明文字占比。
- 生成完成后默认按“主图、场景、质感、卖点”的顺序展示。
- 结果卡片中的 `800×800` 改成真实输出规格或不显示规格，避免被懂行用户看出细节不一致。

### P2：准备 demo 输入约束

- 在 demo 前固定使用一张清晰、单瓶、浅色包装、正面无遮挡的护肤品图。
- Brief 预填简单安全卖点，例如“日常保湿、清爽不黏、适合早晚护理”。
- 不在 demo 中展示食品/家居/服饰等按钮，避免用户以为全品类都已经可用。

## 不建议现在做的复杂优化

- 不建议现在做多平台完整适配。淘宝、抖音、小红书的规范差异会扩大范围，不能解决当前 demo 的第一观感问题。
- 不建议现在接复杂模板系统或模板市场。当前问题不是模板数量少，而是默认模板太重。
- 不建议现在做批量上传、账户体系、支付或用量统计。这些不会改善录屏中的图像可信度。
- 不建议现在引入新的图像模型或复杂依赖。先把 prompt、场景和 Canvas 样式收窄，成本最低。
- 不建议现在做自动抠图/分层合成的大重构。产品保真可以先通过更保守的 prompt 和更少重绘来改善。
- 不建议现在扩展非护肤/美妆品类。Demo 应该先用最稳定品类建立“这个工具能用”的信任。

## 建议确认后的修改顺序

如果要进入代码修改，建议按下面顺序确认：

1. 是否接受 demo 模式下减少 Canvas 文案，尤其主视觉图不再压大标题。
2. 是否将“质地/妆感图”临时改名和改 prompt 为“包装质感/细节图”。
3. 是否固定 demo 场景组合为白底、浅灰台面、浴室白瓷台面、轻文案卖点图。
4. 是否隐藏或弱化未生效的品类、平台、语言和输出模式控件。

确认后可在不重构、不换技术栈、不新增依赖的前提下完成一轮 demo 优先优化。
